# arch-unattended
This script will install arch with a few user inputs to make it more seamless
curl -sSL https://raw.githubusercontent.com/ppkcomputers/arch-unattended/main/make_zero_touch_usb.sh | sudo bash
